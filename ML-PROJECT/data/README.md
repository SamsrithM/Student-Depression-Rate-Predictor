# Data Folder

This folder contains all CSV datasets used for training and evaluation.

## Files

- `studataset.csv` - Student dataset used for model training
- `survey.csv` - Additional survey data (if needed)

## Usage

The training scripts automatically load data from this folder. Make sure your CSV files are placed here before running training scripts.

